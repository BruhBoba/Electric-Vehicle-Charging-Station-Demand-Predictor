# Electric-Vehicle-Charging-Station-Demand-Predictor

To clone and run this code, you might need to install some libraries. They are listed in requirements.txt.
You can clone this project, and then run "pip install -r requirements.txt" in your terminal
to download all those libraries at one.